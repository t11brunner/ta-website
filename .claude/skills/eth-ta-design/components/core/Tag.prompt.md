Small mono-uppercase pill used for status/metadata (e.g. "slides ↓", "3 viz", "PS due", "soon").

```jsx
<Tag tone="accent">2 viz</Tag>
<Tag tone="dim">soon</Tag>
```

Lifted directly from the source site's `.tag` / `.tag.dim` classes.
