Diagonal-hatch placeholder standing in for a chart, demo, or image not yet built — never a gray box or broken-image icon.

```jsx
<PlaceholderMedia label="root-locus plot" height={130} />
```

Straight from the source site's `.ph` pattern — used for every visualisation/gallery card before the real canvas/demo exists.
