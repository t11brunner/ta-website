Standard top-of-section heading: semibold display title, soft ink note capped at 52 characters wide underneath.

```jsx
<SectionHeader title="Visualisations" note="Interactive demos that accompany the lectures, grouped by topic." />
```

From the source site's `.sec-title` / `.sec-note` pattern, used at the top of every screen.
