A clickable index-list row — no card surface at rest, just a hairline top border; on hover it indents and gets a card-color tint, title turns accent, arrow nudges right.

```jsx
<Row number="05" title="Time-Domain Response" meta="LECTURE · SEP 28, 2026" tags={<Tag>2 viz</Tag>} onClick={openWeek} />
```

Straight from the source site's `.row` (weekly index list) — the primary list pattern of the whole site.
